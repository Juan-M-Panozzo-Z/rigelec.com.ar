export default function Section({children}) {
    return (
        <section
        
        className="container mx-auto px-12 mt-8 rounded-box space-y-2">
            {children}
        </section>
    )
}
